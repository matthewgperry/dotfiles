# dotbot configuration file
- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.config/alacritty/alacritty.yml: alacritty
    ~/.config/autostart/*: autostart
    ~/.config/fish:
- shell:
  - chsh -s $(which fish)
