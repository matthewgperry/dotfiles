# dotbot configuration file
- defaults:
    link:
      create: true
      relink: true

- clean: ['~', '~/.config']

- link:
    ~/.config/alacritty/alacritty.yml:
      path: alacritty.yml
- shell:
  - chsh -s $(which fish)
